# ZUK-3257 Stable Bootstrap Receipt

## Scope

- Linear issue: ZUK-3257
- Branch: `feature/ZUK-3257-stable-bootstrap`
- Public interface: `npx @zukantech/agent install`, update, and doctor verification of protected workflow release schema 2
- Acceptance: verify the signed certification asset and preserve its digest/evidence before installing stable `v1.1.0`; publish `@zukantech/agent@0.1.0` through trusted npm publishing
- Non-goals: distributing protected workflow content through npm or changing GitHub organization membership as the identity authority

## Red Evidence

- `node --test --test-name-pattern='stable install verifies' test/install.test.mjs` failed because schema 2 certification was not accepted or downloaded.
- `node --test --test-name-pattern='doctor revalidates stable' test/doctor.test.mjs` failed because schema 2 release locks and certification evidence were not revalidated.

## Green Evidence

- Stable installs verify the manifest signature before trusting the certification digest, validate exact release/revision/harness/gate results, and preserve `certification.json` plus its digest.
- Doctor supports historical schema 1 pins and revalidates schema 2 certification evidence without rewriting it.

## Validation

- `npm test` — 72 tests passed.
- `npm run validate` — approved public inventory and credential scan passed.
- `npm pack --dry-run --json --cache /private/tmp/zukan-agent-npm-cache` — 18 approved files, package identity `@zukantech/agent@0.1.0`.
- `git diff --check` — passed.
- Default-cache `npm pack` encountered pre-existing root-owned cache files; the isolated-cache rerun passed.

## External Writes

- ZUK-3257 is In Progress from the producer slice.

## Residual Risk

- Live producer `v1.1.0` and npm `0.1.0` publication remain pending their respective green pull requests.

## Handoff

- Independent review is next; publish only through the protected GitHub release and npm trusted-publishing workflows.
